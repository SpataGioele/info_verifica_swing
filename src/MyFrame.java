import javax.swing.*;

public class MyFrame extends JFrame {
    public MyFrame() {
        setTitle("Verifica");
        setDefaultCloseOperation(DISPOSE_ON_CLOSE);
        setSize(500, 400);
    }
}
